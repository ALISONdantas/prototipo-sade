# URL do backend
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1

# Usa o resultado de IA mockado (src/mocks/aiMock.ts) enquanto o
# microsserviço de IA real ainda não existe (issue #43).
EXPO_PUBLIC_USE_AI_MOCK=true
